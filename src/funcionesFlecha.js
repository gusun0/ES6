const nombres = ['carlos','alejandro','manuel','cesar'];


const num_carac = nombres.map(nombre => nombre.length );

/*const num_carac = nombres.map(function(nombre){
	return nombre.length;
});*/

console.log(num_carac);
